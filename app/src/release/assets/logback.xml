<configuration debug="true">
    <!-- Create a logcat appender -->
    <appender name="logcat" class="ch.qos.logback.classic.android.LogcatAppender">
        <encoder>
            <pattern>%msg</pattern>
        </encoder>
    </appender>

    <!-- Write WARN (and higher level) messages to logcat -->
    <root level="WARN">
        <appender-ref ref="logcat" />
    </root>

</configuration>